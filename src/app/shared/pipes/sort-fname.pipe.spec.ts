import { SortFNamePipe } from './sort-fname.pipe';

describe('SortFNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortFNamePipe();
    expect(pipe).toBeTruthy();
  });
});

import { SortLNamePipe } from './sort-lname.pipe';

describe('SortLNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortLNamePipe();
    expect(pipe).toBeTruthy();
  });
});

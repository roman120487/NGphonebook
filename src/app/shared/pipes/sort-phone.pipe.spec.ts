import { SortPhonePipe } from './sort-phone.pipe';

describe('SortPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new SortPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
